{
  "name": "@use-compose/i18next-compose-root",
  "version": "1.0.0",
  "private": true,
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "vitest",
    "dev": "pnpm --filter=nuxt-example dev",
    "dev:vue": "pnpm --filter=vue-example dev",
    "dev:build": "pnpm --filter=nuxt-example build",
    "dev:build:vue": "pnpm --filter=vue-example build",
    "dev:generate": "pnpm --filter=nuxt-example generate",
    "build": "pnpm -r build",
    "preview": "pnpm --filter i18next-compose preview",
    "preview:vue": "pnpm --filter=vue-example preview",
    "docs:dev": "pnpm --filter docs docs:dev",
    "docs:build": "pnpm --filter docs docs:build",
    "docs:preview": "pnpm --filter docs docs:preview",
    "changeset": "changeset",
    "changeset:version": "changeset version && pnpm install --lockfile-only",
    "changeset:publish": "changeset publish",
    "changeset:snapshot": "changeset version --snapshot && changeset publish --tag snapshot",
    "release": "pnpm build && pnpm changeset:publish",
    "release:snapshot": "pnpm build && pnpm changeset:snapshot",
    "lint": "eslint .",
    "format": "prettier --write .",
    "prepare": "husky",
    "typedoc": "typedoc"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@eslint/js": "9.20.0",
    "eslint": "9.20.0",
    "eslint-plugin-vue": "10.0.1",
    "globals": "15.14.0",
    "typescript-eslint": "8.23.0"
  },
  "devDependencies": {
    "@changesets/cli": "2.29.6",
    "@commitlint/cli": "19.8.1",
    "@commitlint/config-conventional": "19.8.1",
    "eslint-config-prettier": "10.0.3",
    "husky": "9.1.7",
    "lint-staged": "16.0.0",
    "prettier": "3.5.3",
    "prettier-plugin-organize-imports": "4.1.0",
    "typescript": "5.9.2",
    "vitest": "3.0.9",
    "typedoc": "0.28.10",
    "typedoc-plugin-markdown": "4.8.1",
    "typedoc-vitepress-theme": "1.1.2"
  },
  "lint-staged": {
    "**/*.{js,ts,tsx}": [
      "eslint --fix"
    ],
    "**/*": "prettier --write --ignore-unknown"
  },
  "engines": {
    "node": ">=18",
    "pnpm": ">=9"
  }
}
